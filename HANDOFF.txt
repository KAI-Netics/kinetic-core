================================================================================
KAI-NETICS SESSION HANDOFF
Date: May 8, 2026 (Full Day Session — CLOSED)
================================================================================

RESUME LINE:
"Read my handoff file at C:\Kinetic_Core\HANDOFF.txt and pick up from there."

================================================================================
PLATFORM STATUS
================================================================================
WhatsApp Bridge v4 — ACTIVE
Nova Proactive v3.2 — ACTIVE (SAM skipped-no key, SBIR err, Grants err)
Nova Pursue Command — ACTIVE (BUG: upsert fix pending)
Jordan v3.2 — ACTIVE
DASHBOARD v4.5 — LIVE (225,443 bytes)
LOCAL SUPABASE — LIVE at http://localhost:8000 (empty — no schema yet)
CLOUD SUPABASE — UNHEALTHY (us-east-1a outage all day May 8)

================================================================================
DATABASE GOVERNANCE — PERMANENT
================================================================================
CLOUD SUPABASE = PRODUCTION SYSTEM OF RECORD. NEVER REPLACED.
LOCAL SUPABASE = DEV/MIRROR ONLY. NOT AUTHORITATIVE.
NO SCHEMA OR DATA MIGRATION WITHOUT BACKUP + SYNC RULES + KEVIN APPROVAL.
NOTHING LEAVES THE PLATFORM WITHOUT KEVIN'S EXPLICIT APPROVAL.

================================================================================
DASHBOARD v4.5 — WHAT IS BUILT
================================================================================
✅ Dossier Engine (6-tab: Overview, Source, Contacts, Action Plan, Log, Export)
✅ Disconnected Ops Mode (status strip, sync queue, snapshot export)
✅ Source Intelligence panel (provenance badge, confidence bar, raw payload drawer)
✅ 4 source action buttons (Open Source, Copy Raw, Export Intel Packet, Rebuild)
✅ Page Navigator (floating right-side scroll nav)
✅ Resilient pipeline loading — AUTO-FAILOVER priority:
     1. Cloud Supabase with rows (PRODUCTION FEED)
     2. Local Supabase with rows (LOCAL MIRROR)
     3. Local snapshot with rows (DEGRADED — latest snapshot)
     4. No data anywhere (manual entry mode + Retry)
✅ Feed mode banner — green/amber/yellow per source
✅ SOURCE GAP badges on pipeline items and dossier headers
✅ CONTACT READY / PURSUE NOW / VALIDATE FIRST badges
✅ SOURCE GAP auto-creates research tasks in Action Plan
✅ Morning Brief cards clickable — open dossiers or create stub dossiers
✅ "Missing source data = research task, not dead card" — enforced in code

Deploy method:
  Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
  & "C:\Kinetic_Core\deploy_dashboard.ps1"

================================================================================
WHY PIPELINE IS EMPTY RIGHT NOW
================================================================================
Root cause: Supabase us-east-1a outage all day May 8, 2026.
  - Cloud Supabase: error 521 / ECONNREFUSED all session
  - All n8n agent writes to Supabase failed
  - No pipeline rows ever loaded → no local snapshot saved
  - Scout fired tonight but "No response from Claude" — Anthropic API call failed

Result: Pipeline shows "No pipeline data available" — correct behavior, not a bug.

================================================================================
FIRST ACTIONS NEXT SESSION
================================================================================
1. TEST SUPABASE HEALTH:
   Invoke-WebRequest -Uri "https://raxpmeltyxmtiihxoxpa.supabase.co/rest/v1/pipeline?select=id,title&limit=3" -Headers @{"apikey"="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJheHBtZWx0eXhtdGlpaHhveHBhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY3ODI5MDIsImV4cCI6MjA5MjM1ODkwMn0.UScReg6BG4pimbjMeIE7NB2glcpdbgUbDLnDhDLURPM"} -UseBasicParsing

2. IF SUPABASE HEALTHY:
   - Open dashboard → Platform Ops → click ↻ Refresh
   - Pipeline should load and save local snapshot
   - All dossier/source intelligence features become testable

3. CHECK SCOUT ANTHROPIC API KEY:
   - "No response from Claude" = API key may need rotation
   - n8n → Credentials → find Anthropic key → verify it works
   - Test: fire Scout with a simple query

4. ADD SAM_API_KEY:
   - Get from sam.gov/profile
   - Add to C:\Kinetic_Core\.env
   - Add to START_KINETIC.ps1

================================================================================
TRACK 2 — SCOUT UPGRADE (next major build)
================================================================================
Scout must capture and store per opportunity:
  source_url, source_title, source_platform, discovered_at, discovered_by_agent,
  search_query, raw_payload, raw_snippet, contact_name, contact_title,
  contact_email, contact_phone, organization, program_office, application_portal,
  eligibility_notes, why_this_matters, why_kai_fits, recommended_next_action,
  pursuit_steps, risks, blockers, confidence_score, follow_up_script,
  outreach_draft

Scout classification labels:
  PURSUE_NOW / VALIDATE_SOURCE / CONTACT_READY / WATCHLIST / BLOCKED / ARCHIVE

New priority scoring:
  revenue_potential + immediacy + contactability + source_quality +
  KAI_fit + ease_of_next_action + deadline_urgency + prob_of_response + cert_fit

================================================================================
CRITICAL ADMIN — BLOCKING REVENUE
================================================================================
  MBE/DBE cert    — due May 15 — 7 DAYS — mbe.mdot.maryland.gov
  Techstars       — Demo Day Jun 3 — register by May 15 — 7 DAYS
  OMB M-26-02     — May 20 — FIRST DOLLAR — $7,500/5 days
  SAM.gov UEI     — submitted Apr 17 — confirm active
  MONK LLC        — OVERDUE — businessexpress.maryland.gov
  IRS Form 1023   — OVERDUE — James Scott owns this
  James Scott     — (410) 908-4454 — CALL NOW

================================================================================
BACKLOG (prioritized)
================================================================================
(1)  Verify Supabase cloud healthy + load pipeline snapshot
(2)  Fix Scout Anthropic API key ("No response from Claude")
(3)  Add SAM_API_KEY to .env + START_KINETIC.ps1
(4)  Track 2: Scout schema upgrade (source, contact, raw payload, pursuit fields)
(5)  Environment mode selector (CLOUD/LOCAL/HYBRID toggle in dashboard)
(6)  Cloud schema pg_dump backup (requires Supabase healthy)
(7)  Nova duplicate insert fix (upsert on title+entity)
(8)  Fix recheck button — add toast confirmation
(9)  Fix pooler/realtime/vector containers in local Supabase
(10) Add local Supabase to pm2 startup
(11) Jordan Grant Reader v1 (pending 4 answers)
(12) KAI_Context v3.0 + Scribe protocol
(13) Scout v5 profile-driven reasoning
(14) BD Outreach Engine
(15) Supabase SECURITY DEFINER → INVOKER fix
(16) scout_reports table schema fix
(17) MONK SBIR narrative

================================================================================
LOCAL SUPABASE CONFIG
================================================================================
Location:   C:\Kinetic_Core\supabase\
Start:      cd C:\Kinetic_Core\supabase && docker compose up -d
Stop:       cd C:\Kinetic_Core\supabase && docker compose down
REST API:   http://localhost:8000/rest/v1/
Studio:     http://localhost:8000 (kevin / KAINetics2026!)
DB:         localhost:5432 (postgres / bNK0jMXQvygPNYisNyCaGheyVDVexpKk)
Status:     kong, db, studio, auth, rest, storage, meta, imgproxy healthy
            pooler, realtime, vector, edge-functions still restarting (non-blocking)

================================================================================
TECHNICAL NOTES
================================================================================
Dashboard localStorage keys:
  kai_dossiers_v1         — dossiers keyed by pipeline ID
  kai_sync_queue          — pending/failed/synced cloud operations
  kai_pipeline_snapshot_v1 — last successful pipeline load

Dashboard file: C:\Kinetic_Core\DASHBOARD.html (v4.5, 225,443 bytes)
Backup:         C:\Kinetic_Core\DASHBOARD_pre_dossier.html (v4.2)

n8n camelCase normalization (always needed between nodes):
  const pipelineId = input.pipelineId || input.pipelineid || input.pipeline_id || null;

Jordan Grant Reader pending answers:
  Q1: Gmail OAuth scope includes calendar?
  Q2: Grant Inbox path: C:\Kinetic_Core\Data\Grant_Inbox\ ?
  Q3: Calendar target — primary Gmail or KAI-Netics Ops calendar?
  Q4: Attach PDF to invite? (Recommended: No)

================================================================================
GOVERNANCE — PERMANENT
================================================================================
CLOUD SUPABASE IS THE PRODUCTION SYSTEM OF RECORD. NEVER REPLACE IT.
LOCAL SUPABASE IS DEV/MIRROR ONLY. NEVER TREAT AS AUTHORITATIVE.
NO SCHEMA OR DATA MIGRATION WITHOUT BACKUP + SYNC RULES + KEVIN APPROVAL.
MISSING SOURCE DATA = RESEARCH TASK, NOT A DEAD CARD.
PRODUCTION OUTAGES MUST DEGRADE THE FEED, NOT THE BUSINESS.
NOTHING LEAVES THE PLATFORM WITHOUT KEVIN'S EXPLICIT APPROVAL.
================================================================================
